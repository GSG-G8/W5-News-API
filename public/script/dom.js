const creatNews = (data) => {
  const news = document.createlement('div')
  new.class = 'news';
  const headLine = document.createlement('h3').class = 'news-h3';
  const newsImg = document.createlement('img').class = 'news-img';
  const newsDetail = document.createlement('p').class = 'news-p'
  const newsDate = document.createlement('p').class = 'news-date';
}